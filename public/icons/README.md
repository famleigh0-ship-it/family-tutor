# Icon placeholders

`icon-192.png` and `icon-512.png` are 1x1 placeholder PNGs so the manifest and
build are valid. Replace both with real square icons (192x192 and 512x512,
maskable-safe padding) before shipping — no image tooling was available in
this environment to generate real artwork.
